#ifndef _FLASH_MENU_H_
#define _FLASH_MENU_H_

void create_flash_menu(menu_ *menu);
void create_flash_color_menu(menu_ *menu);

void change_flash(menu_ *menu);
void change_flash_colors(int menu_pos);

#endif
